{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Download LittleFS → data/",
            "type": "shell",
            "command": "pio run -e controller_wemos -t downloadfs",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}
