// Amalgamation TU — the native env doesn't auto-compile library .cpp, so pull the pure
// tree-building logic in here directly (same pattern as test_panel_frame_dispatcher/_engine.cpp).
#include "Core/Relay/DiscoveryTreeBuilder.cpp"
